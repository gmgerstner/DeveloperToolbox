// good candidate: https://www.npmjs.com/package/rfc4648
// Not implemented yet - the Base32 options stay commented out in the UI.

export class Base32Encoder {
  encode(text: string): string {
    return text;
  }

  decode(text: string): string {
    return text;
  }
}
